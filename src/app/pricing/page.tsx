// PRICING PAGE — temporarily hidden. Uncomment below to restore.
// To re-enable: remove the redirect and uncomment the full page component.

import { redirect } from "next/navigation";

export default function PricingPage() {
  redirect("/");
}

/*
FULL PRICING PAGE CONTENT PRESERVED BELOW — restore when needed.

import type { Metadata } from "next";
import Link from "next/link";
import Navbar from "@/components/layout/Navbar";
import Footer from "@/components/layout/Footer";

// ... full pricing page was here
*/
